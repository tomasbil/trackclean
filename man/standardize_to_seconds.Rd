% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/standardize_to_seconds.R
\name{standardize_to_seconds}
\alias{standardize_to_seconds}
\title{Standardize location data to fixed time intervals}
\usage{
standardize_to_seconds(
  data,
  time_col = "At",
  x_col = "X",
  y_col = "Y",
  id_col = "id_code",
  unit = "second",
  verbose = TRUE
)
}
\arguments{
\item{data}{A data frame with location tracking data}

\item{time_col}{Name of the timestamp column (default: "At")}

\item{x_col}{Name of x-coordinate column (default: "X")}

\item{y_col}{Name of y-coordinate column (default: "Y")}

\item{id_col}{Name of ID column (default: "id_code")}

\item{unit}{Time interval to standardize to, passed to
\code{lubridate::floor_date()} (default: \code{"second"}). Use
\code{"2 seconds"}, \code{"5 seconds"}, etc. for coarser intervals.}

\item{verbose}{Print summary statistics (default: TRUE)}
}
\value{
Data frame with one row per (id, interval) with:
\itemize{
\item X, Y: Averaged coordinates
\item n_entries: Number of original signals in that interval
\item standardized: 1 if multiple signals were aggregated, 0 if single
}
}
\description{
Rounds timestamps to the nearest interval boundary and averages X/Y
coordinates if multiple signals fall within the same interval.
}
\examples{
\dontrun{
# Default: 1-second intervals
standardized_data <- standardize_to_seconds(raw_data)

# 2-second intervals
standardized_data <- standardize_to_seconds(raw_data, unit = "2 seconds")
}
}
