% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean_playground_data.R
\name{clean_playground_data}
\alias{clean_playground_data}
\title{Clean tracking data (complete pipeline)}
\usage{
clean_playground_data(
  data,
  id_mapping,
  exclude_ids = NULL,
  analyze_start,
  analyze_end,
  bell_start = NULL,
  bell_end = NULL,
  unit = "second",
  time_step = 1,
  max_gap_small = 10,
  max_gap_large = NULL,
  max_position_change = 0.3,
  output_file = NULL,
  verbose = TRUE,
  time_col = "At",
  x_col = "X",
  y_col = "Y",
  raw_id_col = "ID",
  id_col = "id_code",
  analyze_col = "Analyze",
  bell_col = "Bell"
)
}
\arguments{
\item{data}{Raw tracking data frame}

\item{id_mapping}{Path to ID mapping CSV file or mapping data frame}

\item{exclude_ids}{Vector of raw IDs to exclude from analysis}

\item{analyze_start}{Start time for analysis period (character or POSIXct)}

\item{analyze_end}{End time for analysis period (character or POSIXct)}

\item{bell_start}{Start time for bell period (optional)}

\item{bell_end}{End time for bell period (optional)}

\item{unit}{Time interval for standardization, passed to
\code{standardize_to_seconds()} (default: \code{"second"}). Use
\code{"2 seconds"}, \code{"5 seconds"}, etc. for coarser intervals.}

\item{time_step}{Expected time step in seconds between consecutive observations
after standardization (default: 1). Must match the numeric value of \code{unit},
e.g. set \code{time_step = 2} when \code{unit = "2 seconds"}.}

\item{max_gap_small}{Maximum gap for phase 1 interpolation in seconds (default: 10)}

\item{max_gap_large}{Maximum gap for phase 2 interpolation in seconds (default: NULL)}

\item{max_position_change}{Maximum position change for phase 2 in meters (default: 0.3)}

\item{output_file}{Path to save cleaned data as CSV (optional)}

\item{verbose}{Print progress messages (default: TRUE)}

\item{time_col}{Name of the timestamp column (default: \code{"At"})}

\item{x_col}{Name of the x-coordinate column (default: \code{"X"})}

\item{y_col}{Name of the y-coordinate column (default: \code{"Y"})}

\item{raw_id_col}{Name of the raw device ID column in the input data (default: \code{"ID"})}

\item{id_col}{Name of the output column for standardized participant IDs (default: \code{"id_code"})}

\item{analyze_col}{Name of the analysis period flag column (default: \code{"Analyze"})}

\item{bell_col}{Name of the bell period flag column (default: \code{"Bell"})}
}
\value{
Cleaned data frame
}
\description{
Master function that runs the complete data cleaning pipeline:
\enumerate{
\item Map raw IDs to participant IDs
\item Mark analysis and bell time periods
\item Standardize to fixed time intervals
\item Interpolate gaps (two-phase)
\item Optionally export to CSV
}
}
\examples{
\dontrun{
# Complete pipeline using bundled example data
library(readr)
raw_data <- read_csv(system.file("extdata", "raw_tracking_data.csv",
                                 package = "trackclean"))

cleaned_data <- clean_playground_data(
  data = raw_data,
  id_mapping = system.file("extdata", "id_mapping.csv", package = "trackclean"),
  analyze_start = "2025-03-18 11:47:00",
  analyze_end   = "2025-03-18 11:57:00",
  bell_start    = "2025-03-18 11:53:00",
  bell_end      = "2025-03-18 11:58:00"
)

# Custom column names for a dataset with different structure
cleaned_data <- clean_playground_data(
  data = raw_data,
  id_mapping = "id_mapping.csv",
  analyze_start = "2025-03-18 11:47:00",
  analyze_end   = "2025-03-18 11:57:00",
  time_col    = "timestamp",
  x_col       = "pos_x",
  y_col       = "pos_y",
  raw_id_col  = "tag_id",
  id_col      = "participant_id",
  analyze_col = "in_window"
)
}
}
