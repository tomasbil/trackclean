% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/map_ids.R
\name{map_ids}
\alias{map_ids}
\title{Map raw tracking IDs to standardized child IDs}
\usage{
map_ids(
  data,
  mapping,
  exclude_ids = NULL,
  raw_id_col = "ID",
  id_col = "id_code",
  analyze_col = "Analyze"
)
}
\arguments{
\item{data}{A data frame with raw tracking data}

\item{mapping}{Either:
\itemize{
\item Path to CSV file with columns 'raw_id' and 'child_id'
\item Data frame with columns 'raw_id' and 'child_id'
\item Named vector (raw_id = child_id)
}}

\item{exclude_ids}{Vector of raw IDs to exclude from analysis (sets Analyze = 0)}

\item{raw_id_col}{Name of the raw ID column in data (default: "ID")}

\item{id_col}{Name of the output column for standardized IDs (default: "id_code")}

\item{analyze_col}{Name of the Analyze column in data (default: "Analyze")}
}
\value{
Data frame with added id_code column and updated Analyze column
}
\description{
Map raw tracking IDs to standardized child IDs
}
\examples{
\dontrun{
# Using a CSV file (recommended)
data_mapped <- map_ids(raw_data, "id_mapping.csv", exclude_ids = c(67, 72, 80))

# Using a data frame
id_map <- data.frame(raw_id = c(2, 3, 6), child_id = c(5129, 5113, 5222))
data_mapped <- map_ids(raw_data, id_map, exclude_ids = c(67, 72, 80))
}
}
